<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="per.kirito.pack.mapper.CodeMapper">

	<sql id="FIELDS">
        code, addr, status
    </sql>

	<select id="findCode" parameterType="Code" resultType="ArrayList">
		SELECT code, addr, status
		FROM t_code
		WHERE addr = #{addr}
		AND status = #{status}
	</select>

	<update id="updateCode" parameterType="Code">
		UPDATE t_code
		SET status = #{status}
		WHERE code = #{code}
		AND addr = #{addr}
	</update>
</mapper>